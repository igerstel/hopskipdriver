# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  home_address: one
  # home_address: addresses(:one)

two:
  home_address: two
  # home_address: addresses(:two)
